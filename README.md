```
sudo apt-get install ruby
sudo apt-get install ruby-dev
sudo apt-get install build-essential

sudo gem install em-websocket

cd wtactics-legacy
./launch-servers.sh

open <machine-ip>:8080
```
